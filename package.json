{
  "name": "stock_report",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docs": "node bin/generate_home.mjs && vitepress dev docs --host 0.0.0.0 --port 4174",
    "build": "node bin/generate_home.mjs && vitepress build docs",
    "serve": "node bin/generate_home.mjs && vitepress preview docs --host 0.0.0.0 --port 4174"
  },
  "devDependencies": {
    "vitepress": "^1.0.0"
  }
}
